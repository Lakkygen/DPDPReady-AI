{
  "name": "dpdpready-ai",
  "version": "1.0.0",
  "description": "DPDPReady AI — Multi-agent operations platform for DPDP compliance",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/**/*.test.js",
    "db:migrate": "wrangler d1 migrations apply dpdpready-ai",
    "db:migrate:local": "wrangler d1 migrations apply dpdpready-ai --local"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "private": true
}
